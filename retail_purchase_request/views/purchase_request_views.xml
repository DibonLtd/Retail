<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_retail_purchase_request_form" model="ir.ui.view">
        <field name="name">retail.purchase.request.form</field>
        <field name="model">retail.purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <button name="action_submit" type="object" string="Submit"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve"
                            class="btn-primary" invisible="state != 'submitted'"
                            groups="retail_base.group_procurement_manager"/>
                    <button name="action_create_rfq" type="object" string="Create RFQ"
                            class="btn-primary" invisible="state != 'approved'"
                            groups="retail_base.group_procurement_manager"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state not in ('submitted', 'approved')"
                            groups="retail_base.group_procurement_manager"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state not in ('draft', 'submitted')"/>
                    <button name="action_reset_to_draft" type="object"
                            string="Reset to Draft"
                            invisible="state not in ('rejected', 'cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved,rfq_created"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_orders" type="object"
                                class="oe_stat_button" icon="fa-file-text-o"
                                invisible="order_count == 0">
                            <field name="order_count" widget="statinfo" string="RFQs"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="requestor_id" readonly="state != 'draft'"/>
                            <field name="preferred_vendor_id" readonly="state not in ('draft', 'submitted', 'approved')"/>
                            <field name="date_required" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="amount_estimated" widget="monetary"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="approved_by_id" invisible="not approved_by_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <div class="alert alert-warning" role="alert"
                         invisible="state != 'rejected'">
                        <strong>Rejected by
                            <field name="rejected_by_id" readonly="1" nolabel="1"/>:
                        </strong>
                        <field name="rejection_reason" readonly="1" nolabel="1"/>
                    </div>
                    <notebook>
                        <page string="Products" name="products">
                            <field name="line_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="product_id"/>
                                    <field name="qty_requested"/>
                                    <field name="product_uom_id" readonly="1"/>
                                    <field name="estimated_price"/>
                                    <field name="subtotal" readonly="1"/>
                                    <field name="currency_id" column_invisible="1"/>
                                </list>
                            </field>
                        </page>
                        <page string="Justification" name="justification">
                            <field name="justification" readonly="state != 'draft'"
                                   placeholder="Why is this purchase needed?"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_retail_purchase_request_list" model="ir.ui.view">
        <field name="name">retail.purchase.request.list</field>
        <field name="model">retail.purchase.request</field>
        <field name="arch" type="xml">
            <list string="Purchase Requests">
                <field name="name"/>
                <field name="requestor_id"/>
                <field name="preferred_vendor_id"/>
                <field name="date_required"/>
                <field name="amount_estimated" sum="Estimated Total"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'rfq_created'"
                       decoration-danger="state == 'rejected'"
                       decoration-info="state == 'submitted'"/>
            </list>
        </field>
    </record>

    <record id="view_retail_purchase_request_search" model="ir.ui.view">
        <field name="name">retail.purchase.request.search</field>
        <field name="model">retail.purchase.request</field>
        <field name="arch" type="xml">
            <search string="Purchase Requests">
                <field name="name"/>
                <field name="requestor_id"/>
                <field name="preferred_vendor_id"/>
                <filter name="filter_submitted" string="Awaiting Approval"
                        domain="[('state', '=', 'submitted')]"/>
                <filter name="filter_approved" string="Approved"
                        domain="[('state', '=', 'approved')]"/>
                <group expand="0" string="Group By">
                    <filter name="group_state" string="Status"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_vendor" string="Supplier"
                            context="{'group_by': 'preferred_vendor_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_retail_purchase_request" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="res_model">retail.purchase.request</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_retail_purchase_request_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Raise a purchase request</p>
            <p>Request goods that the central warehouse cannot supply.</p>
        </field>
    </record>
</odoo>
